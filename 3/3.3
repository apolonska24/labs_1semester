#include <iostream> 
using namespace std; 
int main() { 
    int n, k = 0, sum = 0; 
    cout << "Input n value" << endl; 
    cin >> n; 
    while (sum < n) { 
        k++; 
        sum += k; 
    } 
    cout << "for K = " << k << " the sum is equal to " << sum; 
    return 0; 
}
