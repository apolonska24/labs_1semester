#include <iostream> 
#include <cmath> 
using namespace std; 
int main() { 
    int n, k = 0; 
    cout << "Input n value" << endl; 
    cin >> n; 
    while (pow(3, k + 1) < n) { 
        k++; 
    } 
    cout << k; 
    return 0; 
}
