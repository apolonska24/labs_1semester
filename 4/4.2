#include<iostream> 
using namespace std; 
int main() { 
	int n, s = 0; 
	cout << "input N: "; 
	cin >> n; 
	s += n * n; 
	if (n < 0) { cout << "incorrect input"; }
	else {
	    for (int i = n + 1, z = 1; i <= 2 * n; i++, z *= -1) { 
		s += z * i * i; 
	    } 
	cout << s; 
	}
	return 0; 
}
