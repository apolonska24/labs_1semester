#include <iostream> 
using namespace std; 
int main () { 
    int n; 
    cout << "Enter N: " ; 
    cin >> n; 
    float a = 1.0; 
   // for loop execution 
   for(int k = 1; k <= n; k += 1 ) { 
      a = (a + 1)/k; 
      cout << a << endl; 
   } 
   return 0; 
}
