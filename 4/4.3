#include <iostream> 
using namespace std; 
int main () { 
    int a, n; 
    cout << "Enter A, N: " ; 
    cin >> a >> n; 
    int result = 1; 
   // for loop execution 
   for( int i= 1; i <= n; i += 1 ) { 
      result *= a; 
      cout << result << endl; 
   } 
   return 0; 
}
