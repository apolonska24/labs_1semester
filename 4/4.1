#include <iostream> 
using namespace std; 
int main () { 
   int a, b; 
   cout << "Enter A, B" << endl; 
   cin >> a >> b; 
   int result = 1; 
   // for loop execution 
   for( a; a <= b; a = a + 1 ) { 
      result *= a; 
   } 
   if (a<=b) { 
    cout << result; 
   } else { 
       cout << "incorrect input"; 
   } 
   return 0; 
}
