#include <iostream> 
using namespace std; 
int main() { 
    int k, p, t, x, y; 
    cout << "k: " << endl; 
    cin >> k; 
    cout << "p: " << endl; 
    cin >> p; 
    cout << "t: " << endl; 
    cin >> t; 
    x = k; 
    y = p; 
    p = x; 
    k = t; 
    t = y; 
    cout << "k = " << k << " ,p = " << p << " ,t = " << t << endl; 
    return 0; 
}
